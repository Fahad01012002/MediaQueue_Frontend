// app/tutors/layout.js
export const metadata = {
  title: 'All Tutors',
  description: 'Browse all available tutors across different subjects',
};

export default function TutorsLayout({ children }) {
  return <>{children}</>;
}