
export const metadata = {
  title: 'Login',
  description: 'Login to your Tutor Finder account',
  robots: {
    index: false,
    follow: false,
  },
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}